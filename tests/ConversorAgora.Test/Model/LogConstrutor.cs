using ConversorAgora.Model;
using Moq;

namespace ConversorAgora.Test.Model
{
    public class LogConstrutor
    {
        //private readonly Mock<Log> mock;

        //private const 

        [Fact]
        public void RetornaEhValidoDeAcordoComDadoDeEntrada()
        {
            //Log log(provider, httpMethod, statusCode, uriPath, timeTaken, responseSize, cacheStatus);

            //Assert.Equal(validacao, log.EhValido);
        }
    }
}